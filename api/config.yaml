# Models only. Regenerating never touches handler logic.
package: api
output: ./models.gen.go
generate:
  models: true
output-options:
  skip-prune: true
compatibility:
  always-prefix-enum-values: true
