export default function Dashboard() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold text-white">Dashboard</h1>
      <p className="mt-2 text-sm text-gray-400">Current job, queue, awaiting stream end, recent completions, failures and stats. Not built yet (plan.md 18.1).</p>
    </div>
  );
}
