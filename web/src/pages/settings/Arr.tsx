export default function SettingsArr() {
  return (
    <div className="p-8">
      <h1 className="text-2xl font-bold text-white">Radarr and Sonarr</h1>
      <p className="mt-2 text-sm text-gray-400">One or more instances, each with URL, API key, webhook URL and path mappings. Not built yet (plan.md 18.4).</p>
    </div>
  );
}
